canonVersion: 1.1.0
agents:
  - codex
loop:
  enabled: true
  decisionPolicy: auto
runner:
  agent: codex
codeGraph: graphify
verify:
  command: cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all -- --check
